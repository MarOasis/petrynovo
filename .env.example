# Environment variables for distribuidora-aluminio-site
#
# This project currently has no required environment variables.
# Copy this file to `.env.local` if/when a new variable is introduced
# (or configure it in your deployment platform's environment settings,
# e.g. Vercel Project Settings > Environment Variables).
#
# Never commit a real `.env` / `.env.local` file or a real key value to git.
# `.env` is already excluded via .gitignore.
