// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
// };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  // pode manter trailingSlash se quiser, mas não é obrigatório na Vercel
  // trailingSlash: true,
};

export default nextConfig;